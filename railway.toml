[build]
buildCommand = "cd apps/dashboard && npm install --no-audit --no-fund && npm run build:all"

[deploy]
startCommand = "cd apps/dashboard && npm run db:deploy && npm run start"
healthcheckPath = "/api/health"
healthcheckTimeout = 300
