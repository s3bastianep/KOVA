[build]
buildCommand = "npm ci && cd apps/dashboard && npm ci && npm run build:all"

[deploy]
startCommand = "cd apps/dashboard && npm run db:deploy && npm run start"
healthcheckPath = "/api/health"
healthcheckTimeout = 300
